import 'package:flutter/material.dart';
class ExploreLearnScreen extends StatelessWidget {
  const ExploreLearnScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Keşfet ve Öğren modu')));
}

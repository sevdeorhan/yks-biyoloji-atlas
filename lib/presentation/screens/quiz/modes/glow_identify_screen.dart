import 'package:flutter/material.dart';
class GlowIdentifyScreen extends StatelessWidget {
  const GlowIdentifyScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Parlayanı Bil modu')));
}

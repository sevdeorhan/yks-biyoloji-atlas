import 'package:flutter/material.dart';
class PinpointScreen extends StatelessWidget {
  const PinpointScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Nokta Atışı modu')));
}
